apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP  # ใช้ภายในคลัสเตอร์เท่านั้น (ให้ Ingress จัดการ External Traffic)
